// Generated by src/build_site.py. The cache is named after a digest of
// what was built, not after the date: two builds on the same day differ, and naming
// the cache after the day let the first one's assets outlive it.
const CACHE = 'events-tracker-b3c627749cf9';
const ASSETS = ['./', './index.html', './manifest.webmanifest',
                './icon-192.png?v=3', './icon-512.png?v=3', './icon-maskable-192.png?v=3', './icon-maskable-512.png?v=3'];

self.addEventListener('install', e => {
  e.waitUntil(caches.open(CACHE)
    .then(c => Promise.allSettled(ASSETS.map(a => c.add(a))))
    .then(() => self.skipWaiting()));
});

self.addEventListener('activate', e => {
  e.waitUntil(caches.keys()
    .then(keys => Promise.all(keys.filter(k => k !== CACHE).map(k => caches.delete(k))))
    .then(() => self.clients.claim()));
});

self.addEventListener('fetch', e => {
  const req = e.request;
  if (req.method !== 'GET' || new URL(req.url).origin !== self.location.origin) return;
  // The page is rebuilt daily, so prefer the network for documents and fall back to
  // the cache when offline. Everything else is immutable enough to serve cache-first.
  if (req.mode === 'navigate') {
    e.respondWith(fetch(req)
      .then(r => { const copy = r.clone();
                   caches.open(CACHE).then(c => c.put(req, copy)); return r; })
      .catch(() => caches.match(req).then(r => r || caches.match('./index.html'))));
    return;
  }
  e.respondWith(caches.match(req).then(hit => hit || fetch(req).then(r => {
    const copy = r.clone();
    caches.open(CACHE).then(c => c.put(req, copy));
    return r;
  }).catch(() => hit)));
});
